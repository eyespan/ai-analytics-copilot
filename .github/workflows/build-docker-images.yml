name:  Docker Image Builder

on:

  push:
    branches:
      - level7
    paths:
      - "infra/base-images/ml-base/**"

  workflow_dispatch:

permissions:
  contents: read

jobs:

  docker-build:

    uses: ./.github/workflows/docker-build.yml

    with:

      service: all

    secrets: inherit