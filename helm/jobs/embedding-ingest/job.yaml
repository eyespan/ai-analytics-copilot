apiVersion: batch/v1
kind: Job

metadata:
  name: embedding-ingest

spec:

  backoffLimit: 3

  template:

    spec:

      restartPolicy: Never

      containers:

        - name: embedding-ingest

          image: ${ECR_REGISTRY}/embedding-ingest:${IMAGE_TAG}

          imagePullPolicy: Always