apiVersion: v1
kind: Service

metadata:
  name: clickhouse
  namespace: data

spec:

  selector:
    app.kubernetes.io/component: shard

  ports:

    - name: native
      port: 9000
      targetPort: 9000

    - name: http
      port: 8123
      targetPort: 8123